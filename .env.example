# Your Google Calendar ID
# Can be found in calendar settings or using check-setup.ts script
GOOGLE_CALENDAR_ID=your_calendar_id@group.calendar.google.com

# Path to OAuth credentials file
# If not specified, 'oauth-credentials.json' in the project root will be used
# GOOGLE_CREDENTIALS=oauth-credentials.json

# Telegram bot token obtained from @BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCDEFGHIJKLMNOPQRSTUVWXYZ

# Telegram channel or group ID where notifications will be sent
# For channels use format: @channel_name or -1001234567890
# For groups use numeric ID (can be obtained via @getidsbot)
TELEGRAM_CHANNEL_ID=@your_channel_name

# Notification sending time (default 9:00 AM)
NOTIFICATION_HOUR=9
NOTIFICATION_MINUTE=0

# Application mode (development or production)
# In development mode, additional logging is enabled
NODE_ENV=development